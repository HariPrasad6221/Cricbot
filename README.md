# Cricbot
This project builds a small-scale robot that autonomously hits a ball using computer vision to detect its motion. The bot moves along the X-axis to align with the ball and along the Z-axis to swing the bat when the ball approaches.
